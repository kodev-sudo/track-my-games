{% extends 'base.html.twig' %}

{% block title %}Game{% endblock %}

{% block body %}
    <h1>Game</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ game.logo }}</td>
            </tr>
            <tr>
                <th>Background_image</th>
                <td>{{ game.backgroundImage }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ game.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_crud_index') }}">back to list</a>

    <a href="{{ path('game_crud_edit', {'id': game.id}) }}">edit</a>

    {{ include('game_crud/_delete_form.html.twig') }}
{% endblock %}
